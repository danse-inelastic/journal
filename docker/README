To build src rpm:

 $ git clone <repo>/journal
 $ mkdir build
 $ cd build
 $ cmake ../journal
 $ (sudo) make build-srpm

src rpm will be in docker/srpm/build/SRPMS/

Upload it to release site.

Then test the src rpm by

 $ (sudo) make test-srpm

